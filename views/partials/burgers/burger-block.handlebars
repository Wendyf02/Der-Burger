{{!-- <li>
	{{name}}

	<button class="change-sleep" data-id="{{id}}" data-newsleep="{{sleep}}">
		{{#if sleep}}SLEEP TIME!{{else}}WAKE UP!{{/if}}
	</button>

	<button class="delete-cat" data-id="{{this.id}}">DELETE!</button>
</li> --}}


<div class="mt-4">
         <h4>{{burger_name}}</h4>

<!--Devour time button-->
    <button class="change-devour btn btn-danger" data-id="{{id}}"data-newdevour="{{devour}}">
		 <i class='fas fa-hamburger' style='font-size:24px'></i> Devour Time!
    </button>
 </div>


  