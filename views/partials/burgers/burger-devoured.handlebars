

 <div class="mt-4">
      <h4 class="devoured-burger-name">{{burger_name}}</h4>
                    
      <!--DELETE  burger button-->
         <button class="delete-burger btn btn-danger" data-id="{{this.id}}"><i class="far fa-trash-alt"></i> DELETE</button>
    </div>